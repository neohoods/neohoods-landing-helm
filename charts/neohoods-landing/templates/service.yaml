apiVersion: v1
kind: Service
metadata:
  name: { { include "neohoods-landing.fullname" . } }
  labels: { { - include "neohoods-landing.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: { { .Values.service.targetPort } }
      protocol: TCP
      name: http
  selector: { { - include "neohoods-landing.selectorLabels" . | nindent 4 } }
